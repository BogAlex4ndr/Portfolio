import React from 'react';
import { Document, Page } from 'react-pdf';
import styles from './Resume.module.scss'

const Resume = () => {
  return (
    <div>
      {/* <Document file={}>
        <Page pageNumber={1} />
      </Document> */} there will be my resume
    </div>
  );
};

export default Resume;
